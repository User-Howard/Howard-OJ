#include<iostream>
#include<vector>
using namespace std;

int N, K, P, person;
vector<int >arr;
vector<int >people;
int main(){
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	
	cin>>N>>K>>P;
	for(int i=0, temp;i<N;i++){
		cin>>temp;
		arr.push_back(temp);
	}
	
	while(cin>>person){
		people.
	}
    return 0;
}
