#include<iostream>
#include<vector>
using namespace std;

int N, K, P;
vector<int >arr;
int main(){
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	
	cin>>N>>K>>P;
	for(int i=0, temp;i<N;i++){
		cin>>temp;
		arr.push_back(temp);
	}
	
	while(cin>>){
		
	}
    return 0;
}
