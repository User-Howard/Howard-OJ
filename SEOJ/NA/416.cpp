#include <iostream>
#include <vector>
using namespace std;


const int Mod = 1e9+7;
const int MAX_N = 2002;
int main(){
    int N, A, B, K;
    cin >> N >> A >> B >> K;
    vector<vector<int> > dp(MAX_N, vector<int>(MAX_N, 0));
    for(int )
	return 0;
}

