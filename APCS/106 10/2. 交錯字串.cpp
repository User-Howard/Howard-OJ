#include<bits/stdc++.h>
using namespace std;


int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int N;
    bool arr[(int )1e5];
    string STR;
    cin>>STR;
    N = STR.size();

    for(int i=0;i<STR.size();i++){
        arr[i] = (65<=i && i<=90)
    }
}
