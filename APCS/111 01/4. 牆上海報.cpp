// NF
// https://zerojudge.tw/ShowProblem?problemid=h084
/*
*/
#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;



int main(){
	int N, K;
	cin >> N >> K;
	vector<int> H(N), W(K);
	for(int &i:H) cin >> i;
	for(int &i:W) cin >> i;
	int l=1, r = *max_element(H.begin(), H.end());
	while()
	return 0;
}
